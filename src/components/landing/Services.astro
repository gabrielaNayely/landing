---
import tratamientos from '../../datos/tratamientos.json'

const destacados = tratamientos.slice(0, 4)
---

<section id="tratamientos">

    <div class="bg-celesteprim items-center text-center py-8 px-14 md:px-36">
        <div class="animation w-full pb-8 flex flex-col gap-5">
            <h1 class="text-2xl text-azulprim font-bold">TRATAMIENTOS</h1>
            <p class="">Nuestra clínica dental ofrece una amplia gama de servicios de alta calidad para satisfacer todas tus necesidades dentales. Algunos de nuestros servicios más destacados incluyen:</p>
        </div>

        <div class="animation grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-2 gap-6 p-4">
            {
              destacados.map((tratamiento) => (
                <div class="bg-white rounded-2xl shadow-md overflow-hidden flex flex-col sm:flex-row p-4 gap-4 items-center transition-transform hover:scale-[1.02] duration-300">
          
                  {/* Imagen */}
                  <div class="w-full sm:w-1/2">
                    <img 
                      src={tratamiento.imagen} 
                      alt="Tratamiento" 
                      class="w-full h-auto object-cover rounded-xl p-2"
                    />
                  </div>
          
                  {/* Texto */}
                  <div class="w-full sm:w-1/2 text-center sm:text-left">
                    <h2 class="text-azulprim font-bold text-lg mb-2">{tratamiento.tratamiento}</h2>
                    <p class="text-gray-700 text-sm">{tratamiento.descripcion}</p>
                  </div>
          
                </div>
              ))
            }
          </div>  

          <div class="text-center mt-6">
            <a href="/tratamientos" class="text-azulprim font-semibold hover:text-azuloscuro transition">
              Ver todos los tratamientos
            </a>
          </div>

    </div>

</section>