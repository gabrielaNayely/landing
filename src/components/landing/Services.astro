---
import tratamientos from '../../datos/tratamientos.json'

const destacados = tratamientos.slice(0, 4)
---

<section id="tratamientos" class="scroll-mt-10">

    <div class="bg-custom-pattern01 items-center text-center py-8 px-14 md:px-24">
        <div class="w-full pb-8 flex flex-col gap-5 md:px-16">
            <h1 class="text-2xl text-azulprim font-extrabold mt-4">TRATAMIENTOS</h1>
            <p class="animacion animado">Nuestra clínica dental ofrece una amplia gama de servicios de alta calidad para satisfacer todas tus necesidades dentales. Algunos de nuestros servicios más destacados incluyen:</p>
        </div>

        <div class="flex flex-wrap justify-center gap-5 ">
            {
              destacados.map((tratamiento) => (
                <div class="animacion animado bg-white h-80 w-60 rounded-2xl shadow-md p-4 pt-3 gap-2 overflow-hidden flex flex-col sm:h-48 sm:w-80 sm:flex-row  items-center transition-transform hover:scale-[1.02] duration-300">
          
                  {/* Imagen */}
                  <div class="h-40 sm:w-1/2">
                    <img 
                      src={tratamiento.imagen} 
                      alt="Tratamiento" 
                      class="h-full object-cover rounded-xl"
                    />
                  </div>
          
                  {/* Texto */}
                  <div class="w-full sm:w-1/2 text-center sm:text-left">
                    <h2 class="text-azulprim font-bold text-lg mb-2">{tratamiento.tratamiento}</h2>
                    <p class="text-gray-700 text-sm">{tratamiento.descripcion}</p>
                  </div>
          
                </div>
              ))
            }
          </div>  

          <div class="text-center mt-8">
            <a 
              href="/tratamientos"
              class="inline-block bg-blue-500 text-white font-semibold px-6 py-1 rounded-lg shadow-md transition transform ease-in-out hover:bg-blue-600 hover:scale-105 duration-300"
            >
              Ver todos los tratamientos
            </a>
          </div>
          
          

    </div>

</section>