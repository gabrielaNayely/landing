---
import Layout from "../layouts/Layout.astro";

import tratamientos from "../datos/tratamientos.json" 
---
<Layout>
    <section id="tratamientos" class="bg-celesteprim items-center text-center">
        <h1 class="text-2xl text-azulprim font-bold py-6">TRATAMIENTOS</h1>

        <div class="pb-4 px-14 lg:mx-14 md:px-10 lg:grid lg:grid-cols-3">
            <div class="animation w-full pb-8 flex flex-col gap-5">
              <h3 class="text-2xl text-azulprim">Sonríe siempre</h3>
              <p>Nuestra clínica dental ofrece una amplia gama de servicios de alta calidad para satisfacer todas tus necesidades dentales. Algunos de nuestros servicios más destacados incluyen:</p>
            </div>
          
            <div class="flex flex-wrap justify-center gap-5 lg:col-span-2">
              {
                tratamientos.map((tratamiento) => (
                  <div class="animacion animado bg-white h-80 w-60 rounded-2xl shadow-md p-4 pt-3 gap-2 overflow-hidden flex flex-col sm:h-48 sm:w-80 sm:flex-row  items-center transition-transform hover:scale-[1.02] duration-300">
            
                    {/* Imagen */}
                    <div class="h-40 sm:w-1/2">
                      <img 
                        src={tratamiento.imagen} 
                        alt="Tratamiento" 
                        class="h-full object-cover rounded-xl"
                      />
                    </div>
            
                    {/* Texto */}
                    <div class="w-full sm:w-1/2 text-center sm:text-left">
                      <h2 class="text-azulprim font-bold text-lg mb-2">{tratamiento.tratamiento}</h2>
                      <p class="text-gray-700 text-sm">{tratamiento.descripcion}</p>
                    </div>
            
                  </div>
                ))
              }
            </div>  
        </div> 

        
</Layout>